const registerUser = (email, password, nameUser) => {
    return Promise.resolve()
};

const singIn = (email, password) => {
    return Promise.resolve()
}

const singInGoogle = () => {
    return Promise.resolve()
}

const logout = () => {
    return Promise.resolve()
}


export {registerUser, singIn, singInGoogle, logout}
